---
// const {page, pageText} = Astro.props;
import Navigation from './navigation.astro'
import Menu from './menu.astro'
---
  <!-- <a href={`/${page}`}>{pageText}</a> -->
<header>
  <nav>
    <Menu/>
    <Navigation/>
  </nav>
</header>